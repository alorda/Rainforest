
<% @product.reviews.each do |review| %>
  <p>
    <%= review.comment %>
    <br>
    <% if review.user != nil %>
      <em> By <%= review.user.name %>, <%= review.user.email %></em>
    Added on: <%= review.created_at %>
    <% end %>
  </p>
<% end %>
